// given [3, -2, -1, 0, 2, 4, 1] sort into [-2, -1, 0, 1, 2, 3, 4]

// or given L and R (start and end):

// [3, -2, -1, 0, 2, 4, 1]
//                ^     ^
//                L     R

// [3, -2, -1, 0, 1, 2, 4]

const qs = (arr, l, r) => {
  if (l >= r) {
    return;
  }
};
